#ifndef RFC_SOCKET_H
#define RFC_SOCKET_H
#include <sys/socket.h>
#include <netdb.h>

class RFC_Socket
{
public:
    RFC_Socket(bool ipv6, bool udp);
private:
    int socketfd;
    int newsockfd;
    struct sockaddr_in server_addr;
    struct sockaddr_in client_addr;
    socklen_t client_lenght;
};

#endif // RFC_SOCKET_H
